# Server
PORT=5000

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://username:password@localhost:5432/yourdbname

# CORS — allow only your frontend domain(s). Required in production.
# Comma-separated for multiple origins, or * for all (dev only)
CORS_ORIGIN=http://localhost:3000,https://yoursite.com

# Optional: request body size limit (default 10kb)
# REQUEST_SIZE_LIMIT=10kb

# -----------------------------------------------------------------------------
# Email (Nodemailer) — required for contact form notifications
# Notifications are sent to: servicepathtotechnologies@gmail.com
# -----------------------------------------------------------------------------
# Gmail: use your Gmail address and an App Password (see docs/EMAIL_SETUP.md)
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-app-password

# Optional: defaults work for Gmail
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_SECURE=false

# Optional: from address (defaults to MAIL_USER) and display name
# MAIL_FROM=your-email@gmail.com
# MAIL_FROM_NAME=Service Path Technologies (Contact Form)

# -----------------------------------------------------------------------------
# Admin authentication (JWT)
# -----------------------------------------------------------------------------
# Required for admin login and protected routes (GET /api/contact, DELETE /api/contact/:id)
# Use a long, random string (e.g. openssl rand -base64 32)
JWT_SECRET=your-long-random-secret-at-least-32-chars

# Optional: token expiry (default 7d)
# JWT_EXPIRES_IN=7d

# One-time: create first admin (then remove ADMIN_PASSWORD from .env)
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=your-secure-password
